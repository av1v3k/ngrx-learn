export interface currentUserI {
    id?: number;//optional ?
    email: string;
    createdAt?: string; //optional ?
    updatedAt?: string; //optional ?
    username: string;
    bio?: string;
    image: string;
    token: string;
}